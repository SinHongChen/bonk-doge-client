import styled from "styled-components";
import { deviceMedia } from "styles/Device";

export const DeckSelectSectionContainer = styled.div`
    height: 100%;
    background-color: var(--bg-color-1);
    width: 100%;
    display: flex;
    grid-template-rows: 500px;
    position: relative;
    grid-gap: 10px;
    align-items: center;
    justify-content: center;
    padding: 20px;
    flex-wrap: wrap;
`